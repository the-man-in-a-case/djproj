- name: {{ industry }}
  image: alpine:3.20
  command: ["sh","-c","echo default {{industry}} v{{version}}; sleep 3600"]
  env:
    - name: INDUSTRY_VERSION
      value: "{{ version }}"
  volumeMounts:
    - name: cm-{{industry}}
      mountPath: /etc/{{industry}}
